---
/**
 * * Pricing cards with three cards
 */

import Button from "@components/Button/Button.astro";
import { Image } from "astro:assets";

interface FacilityItem {
	icon: string;
	title: string; // plan name
}

import image1 from "@assets/images/sala.jpeg";
import image2 from "@assets/images/tv.jpeg";
import image3 from "@assets/images/hab-1.jpeg";
import image4 from "@assets/images/hab-2.jpeg";
import image5 from "@assets/images/bajada.jpeg";
import image6 from "@assets/images/juegos.jpg";
import type { ImageMetadata } from "astro";

interface FacilityItem {
	icon: string;
	title: string;
	image?: ImageMetadata;
	featured: boolean;
}

const facilitiesData: FacilityItem[] = [
	{
		icon: "🏠",
		title: "Capacidad para 7 personas ",
		featured: false,
		image: image1,
	},
	{
		icon: "❄️",
		title: "A/A en las habitaciones",
		featured: false,
		image: image3,
	},
	{
		icon: "🛜",
		title: "TV con Flow y Wifi",
		featured: false,
		image: image2,
	},
	{
		icon: "🛌",
		title: "Ropa de cama incluída",
		featured: false,
		image: image4,
	},
	{
		icon: "🧴",
		title: "Amenities (shampoo, crema de enjuague, jabón, aceite, sal, café, té, etc.)",
		featured: false,
	},
	{
		icon: "🏖️",
		title: "Bajada a la playa a 50 mts",
		featured: false,
		image: image5,
	},
	{
		icon: "🗝️",
		title: "Ubicada en complejo privado, máxima seguridad",
		featured: false,
	},
	{
		icon: "🛝",
		title: "Espacios verdes y plaza de juegos para chicos",
		featured: false,
		image: image6,
	},
];
---

<section id="facilities" class="mx-auto max-w-6xl scroll-mt-10 px-4 py-8 md:py-12">
	<div class="mx-auto mb-8 max-w-6xl text-center">
		<h2 class="h2 text-pretty uppercase">Nuestros servicios y facilidades</h2>
	</div>
	<div class="grid grid-cols-1 gap-6 md:grid-cols-2 md:gap-4">
		{
			facilitiesData.map((facility, i) => (
				<div
					class:list={[
						"flex flex-col gap-3 md:gap-4",
						i % 2 === 0 ? "md:flex-row" : "md:flex-row-reverse",
					]}
				>
					{facility.image && (
						<div class="w-full md:w-2/3">
							<div class="relative aspect-video w-full overflow-hidden rounded-md">
								<Image
									src={facility.image}
									alt={facility.title}
									width={450}
									densities={[1.5, 2]}
									class="absolute inset-0 h-full w-full object-cover object-center"
								/>
							</div>
						</div>
					)}
					<div class:list={["flex items-center", facility.image ? "w-full md:w-1/3" : "w-full"]}>
						<h3 class="h3 text-center md:text-left">
							{facility.icon}
							{facility.title}
						</h3>
					</div>
				</div>
			))
		}
	</div>

	<div class="mt-12 flex justify-center">
		<Button href="/#contact" variant="outline" class="px-8">Contáctanos</Button>
	</div>
</section>
